# Allowlist : faux positifs documentaires (exemples JWT dans APIDOCUMENTATION).
# Le CLI scanne aussi l'historique Git ; ce fichier a contenu des tokens
# d'exemple qui ne sont pas de vrais secrets de production.
title = "sodalis-frontend"

[allowlist]
description = "Documentation examples (JWT placeholders)"
paths = [
  '''(^|/)APIDOCUMENTATION\.md$''',
]
